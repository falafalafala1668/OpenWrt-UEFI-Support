--- target/linux/x86/base-files/lib/upgrade/platform.sh	2020-02-12 14:29:17.000000000 +0800
+++ OpenWrt-UEFI-Support/src/target/linux/x86/base-files/lib/upgrade/platform.sh	2020-02-13 22:30:56.109588288 +0800
@@ -3,17 +3,17 @@
 	[ "$#" -gt 1 ] && return 1
 
 	case "$(get_magic_word "$1")" in
-		eb48|eb63) ;;
+		eb48|eb63|16fa) ;;
 		*)
 			echo "Invalid image type"
 			return 1
 		;;
 	esac
 
-	export_bootdevice && export_partdevice diskdev 0 || {
-		echo "Unable to determine upgrade device"
-		return 1
-	}
+	# export_bootdevice && export_partdevice diskdev 0 || {
+	# 	echo "Unable to determine upgrade device"
+	# 	return 1
+	# }
 
 	get_partitions "/dev/$diskdev" bootdisk
 
